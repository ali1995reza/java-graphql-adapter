fragment fr on Bus {
    model
    produceYear @MD5(salt: "salt")
    size
}

query @Authentication(token:"some_token") {
    getVehicle(isCar: false) {
        __typename
        ... on Car {
            model
            produceYear
        }
        ... fr
    }
}